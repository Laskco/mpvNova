<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver
            android:name=".BenchmarkProbeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="app.mpvnova.player.BENCHMARK_QUERY_PLAYBACK" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
